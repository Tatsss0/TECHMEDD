// Service Worker Registration - Unified (handled by notification-manager.js)
// Note: Service worker is now registered by notification-manager.js during user authentication
// This ensures proper initialization order: Firebase Auth → User Login → Notification Setup

console.log('✅ TECHMED PWA - Ready for service worker registration via notification-manager.js');